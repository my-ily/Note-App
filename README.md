# Note App 📝

A small, responsive Note-taking web app built with React.  
This project is designed as a learning / portfolio project and demonstrates common frontend skills: CRUD with `localStorage`, responsive UI, dark mode, categories/tags, search, and simple state management.

---

## Features

- ✅ Create, Read, Update, Delete (CRUD) notes  
- ✅ Persistent storage using `localStorage`  
- ✅ Dark / Light mode toggle  
- ✅ Responsive grid layout (cards) with fixed card height + internal scroll  
- ✅ Categories or tags for filtering notes  
- ✅ Search notes by text  
- ✅ Edit notes inline or via modal  
- ✅ Delete notes (single)  
- ✅ Small UI animations and accessible controls

---

## Demo

Add a short screenshot or GIF here (recommended) to show the UI.

---

## Tech stack

- React (functional components + hooks)  
- TailwindCSS for styling  
- react-icons for icons  
- localStorage for persistence  
- Optional: Context API or `useReducer` for state management

---

## Project structure (suggested)

